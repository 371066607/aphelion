# 正式异星家园验证记录（2026-09-12）

普通入口：仓库根目录 `game.html`。`?autostart=1` 仅跳过开场，不赠送建筑或床。所有浏览器检查使用独立临时 Chrome profile。

| 验证 | 结果 | 边界 |
| --- | --- | --- |
| 构建 | `python3 build.py` 成功，40896KB | `game.html` 自动生成 |
| 单元 | 989 / 0 失败 | 纯逻辑及存档迁移 |
| 场景 | 122 / 0 失败 | 包含双世界300模拟秒、生活及生产链 |
| 独立关键场景 | 6 项通过 | 预约、携带者移除、拆除退款、真实有限矿采集返航、战时室内补燃、低技能居民实际修墙恢复房间 |
| 性能护栏 / Boss / UI | 5 / 7 / 95 全通过 | 桩环境回归与交互断言 |
| 两房原型 | 17 模型用例 + boot/UI 通过 | 原型隔离、键盘控制 |
| [四方向正式家具](formal-building.json) | 四朝向床及连续墙门，共14项落成 | 实际Chrome Canvas，无限建造夹具；非生存验收 |
| [相机与地图](camera.json) | 50% / 200% 实际鼠标放置、总览定位通过 | 放置夹具启用无限建造；普通入口另独立取基线 |
| [双世界浏览器](dual-world.json) | 队员选择、移动、重载、幂等返航通过 | 货物为夹具，不等同整季游玩 |
| [实际帧性能](performance.json) | 128格、20居民、超过2000世界对象，120个实际渲染帧 | Chrome 152 无头模式；记录每帧CPU、帧间隔、寻路、缓存和存档量；不是人类交互延迟测量 |

浏览器未捕获运行时异常。`ordinary-home.png` 是真实新档，`map-overview.png` 是地图定位夹具。所有长测结论须以独立季节记录为准；跳日检查仅验证季节边界与里程碑条件，不证明生存平衡。

仍需人工从普通入口操作并完整游玩：建设手感、地图密度、长期经营难度，以及首夜到扎根的连续体验。自动测试和截图不替代这项验收。

[六日长测](season.json)：固定种子9301，144000步×0.05秒×三倍速，实际21600模拟秒，命令exit=0。三人末尾健康、家园房温21℃；真实远征持续300秒并返航5铁矿，粮荒恢复，观察到708次补燃。保留家园袭击，通过正常征召/移动/俘虏命令防守；远征段隔离随机战斗，验证真实采矿和物流。本次自然强攻未毁墙，实际毁墙重建由独立关键场景第6项验证。

这是有初始燃料储备的预建基地自动场景，不是新档从零建设的一季人工游玩。`season-before-builder-fix.json` 是明确未通过的历史失败样本，不能当成最终验收结果。

最终浏览器性能记录：每帧CPU P50 2.7ms、P95 5.9ms、最大6.4ms；帧间隔P95 16.7ms。20次寻路合计52.60000014305115ms，存档743481字节，无捕获异常。源码及构建指纹见 `build-fingerprint.json`。
